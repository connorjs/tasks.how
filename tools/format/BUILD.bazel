load("@rules_shell//shell:sh_binary.bzl", "sh_binary")

package(default_visibility = ["//visibility:public"])

sh_binary(
    name = "prettier",
    srcs = ["prettier.sh"],
    data = [
        "@npm//:prettier_bin",
    ],
)
