# Force LF in repo and on checkout for all text files
* text eol=lf

# Treat these as binary (no EOL conversion)
*.png binary

# Treat these as generated (GitHub will automatically collapse the diff)
*.gen.* linguist-generated
