# gazelle:ignore
load("@rules_typescript//npm:defs.bzl", "node_modules")
load("@rules_typescript//ts:defs.bzl", "ts_bundle")
load("@rules_typescript//vite:bundler.bzl", "vite_bundler")

package(default_visibility = ["//visibility:public"])

# TODO: AI: Confirm this is generated from Gazelle? Or is this specific one not (the ignore at the top)? Why/Why not?
node_modules(
    name = "node_modules",
    deps = [
        "@npm//:react",
        "@npm//:react-dom",
        "@npm//:vite",
    ],
)

# TODO: AI: Document how to run the SPA in the README.
vite_bundler(
    name = "vite",
    node_modules = ":node_modules",
    vite = "@npm//:vite",
)

ts_bundle(
    name = "board_bundle",
    bundler = ":vite",
    entry_point = "//experiences/board/ui/src:main",
    html = "index.html",
    minify = False,
    mode = "app",
    sourcemap = True,
    staging_srcs = [
        "index.html",
        "//experiences/board/ui/src:sources",
    ],
)
