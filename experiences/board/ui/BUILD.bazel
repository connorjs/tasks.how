# gazelle:ignore
load("@rules_typescript//npm:defs.bzl", "node_modules")
load("@rules_typescript//ts:defs.bzl", "ts_bundle")
load("@rules_typescript//vite:bundler.bzl", "vite_bundler")

package(default_visibility = ["//visibility:public"])

node_modules(
    name = "node_modules",
    deps = [
        "@npm//:react",
        "@npm//:react-dom",
        "@npm//:vite",
    ],
)

vite_bundler(
    name = "vite",
    node_modules = ":node_modules",
    vite = "@npm//:vite",
)

ts_bundle(
    name = "board_bundle",
    bundler = ":vite",
    entry_point = "//experiences/board/ui/src:main",
    html = "index.html",
    minify = False,
    mode = "app",
    sourcemap = True,
    staging_srcs = [
        "index.html",
        "//experiences/board/ui/src:sources",
    ],
)
